import React, { use } from 'react';

const UserDetail2 = ({userPromise}) => {
const {name,userNam} = use(userPromise)

  return (
    <div>
      <p><small></small></p>
    </div>
  );
};

export default UserDetail2;